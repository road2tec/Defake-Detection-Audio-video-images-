# MongoDB Connection String
# Replace with your actual MongoDB Atlas connection string
MONGODB_URL=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/?retryWrites=true&w=majority

# Database Name (Optional, defaults to deepfake_db if not set)
DB_NAME=deepfake_db

# Optional: Port for the backend server (default is usually 8000 or 8080)
PORT=8080
